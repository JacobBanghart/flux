apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Bundled manifest: CRD + Namespace + ServiceAccount + RBAC + PriorityClasses + Deployment + Service
  - https://github.com/kubevirt-manager/kubevirt-manager/releases/download/v1.5.4/bundled-v1.5.4.yaml
  # Custom ingress (replaces the upstream nginx-only ingress-v1.5.4.yaml)
  - ingress.yaml
