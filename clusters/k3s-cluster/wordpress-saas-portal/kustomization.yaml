apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - postgresql-init.yaml
  - postgresql.yaml
  - rbac.yaml
  - portal.yaml
  - provisioner.yaml
  - ingress.yaml
  - cloudflared.yaml
  # Note: Secrets are NOT included in this kustomization.
  # Apply secrets from serverk8sdeploywithk3s repo:
  #   kubectl apply -f wordpress-saas-portal.secret.yaml
